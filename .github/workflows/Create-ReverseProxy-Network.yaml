name: Create Reverse Proxy Network

on:
  workflow_dispatch:

jobs:
  do-nothing-this-is-tmp:
    name: This does nothing and is just a placeholder
    runs-on: ubuntu-latest

    steps:
      - name: No-op
        run: echo "Did nothing and returning now :)"
